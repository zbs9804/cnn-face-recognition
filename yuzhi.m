%求平均脸
function B=yuzhi(img)
B=reshape(img,10000,1);

%YUZHI 此处显示有关此函数的摘要
%   此处显示详细说明
% img = imread(path);
% 
%  y1 = img(:,:,2);
%  y2 = img(:,:,3);
% 
% outcb = mean(y1(:));
% outcr = mean(y2(:));

% end

